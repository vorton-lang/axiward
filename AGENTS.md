# 项目工作约定

- `axiward` 是本项目唯一开发仓库。开发工作在本仓库推进，符合下述必要性标准的内容及时提交、同步。
- **非必要不入库。** 任何内容只有在丢弃它会导致后续开发出错或无法继续时，才允许加入或保留在 Git 仓库中。必须能指出具体会造成什么错误或阻碍；不满足这一条件的内容不应入库，已经存在的应当删除。此原则适用于代码、原型、实验脚本、文档、设计决定、进度、日志和证据；“可能有用”“保留历史”“方便追溯”本身不是保留理由。
- 必要的检查或实验脚本放在 `Tests/` 或 `experiments/`。可再生的大型运行目录放在仓内 `.work/`，不入 Git；输入、结果、失败原因和证据同样按必要性标准筛选，不整批归档。
- 第三方工具链是依赖，不需要把整套工具链或缓存放入 Git。旧版本通过 Git 历史追溯，不另建归档目录或保留重复副本。
- 当前权限方案是薄适配器加原生 harness 权限原语。
- 用户沟通与进度对齐使用 `user-collaboration` skill。
- 需要保留的权限依据、规格含义、实际证明和运行观测分别记录；不能为了宣称完成而缩小原有目标。
