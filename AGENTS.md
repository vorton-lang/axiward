# 项目工作约定

- `axiward` 是本项目唯一开发仓库。代码、原型、实验驱动、设计决定、进度和验收依据都在本仓库推进并及时提交、同步。
- 新的实验脚本放在 `experiments/` 或 `Tests/`；原型源码必须入 Git。可再生的大型运行目录放在仓内 `.work/`，但每次影响结论的尝试必须在仓内保存并提交输入、结果、失败原因及必要证据，不能只引用仓外日志。
- 第三方工具链是依赖，不需要把整套工具链或缓存放入 Git。历史仓外材料的归档见 `history/README.md`。
- 当前权限方案是薄适配器加原生 harness 权限原语。文本只说明流程，不能代替隔离实现，也不能把隔离欠项长期降为可选工作。
- 用户有 ADHD。汇报直接说明当前能力、未完成项、阻碍与下一交付，先给简短状态；技术细节和完整记录用链接按需展开。不要机械套用 PDCA 或用测试数量冒充总体完成度。
- 权限依据、规格含义、实际证明和运行观测分别记录；不能为了宣称完成而缩小原有目标。

用户的全局约定继续适用，尤其不要自动批量或递归删除本地实验目录。
