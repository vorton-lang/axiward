# 当前能力与未完成边界

完整产品要求以[Root.md](../spec/Root.md)为准。产品根 Lean 草稿需用户审核；本次统一接入不注册它、不纳管原仓，也不证明完整 Axiward 已满足根要求。

当前接入使用运行时规格包及 Git 固定版本：一个控制器可加载不同项目，工作包、核验、重启与交接使用原有 Scope/历史绑定。FIFO 满时拒绝、满时覆盖和 Nat 后继例子均为普通输入；旧的固定 FIFO 注册及自身专用分组/局部证明分支已撤除。

当前检查状态见[证明及检查范围](execution-slice.md)。统一入口八项限时验收已通过七项；错版本完整流程因 60 秒预算超时，用户已决定本轮跳过并允许合入。该项不计为通过，测试及历史失败记录保留。历史诊断与旧版本检查记录不替代该项验收。

仍待独立完成：完整产品根的文字/形式含义确认、实现与实际成品的完整对应、真实 agent 持续开发和用户最终验收。此前宏改写/运行替换反例被自动审查阻断，未执行；不换入口重试。权限隔离检查按用户要求跳过。普通例子接纳及运行只证明其明确命题和接入路径。
