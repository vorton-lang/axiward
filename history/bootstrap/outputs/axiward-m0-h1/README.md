# M0-H1 接入调查结果

2026-09-18 · **部分机制已验证；依据用户决定 D6，沙箱问题不再阻塞原型。**

| 实际检查 | 结果 |
| --- | --- |
| 原生 Windows 沙箱读写视图 | 成功 |
| 普通命令直接读写假仓库，包括 `..` 路径 | 被拒绝 |
| 复制 CLI 到视图后访问仓库 | 被拒绝 |
| 按真实 PowerShell 调用设置 `decide` 拒绝规则 | 在原生工具路由中被拒绝 |
| 命令放行规则让 CLI 访问仓库 | 本轮组合没有跑通；CLI 仍以 `CodexSandboxOnline` 身份运行 |
| 原生 MCP 工具调用 Lean CLI | 在限定测试工具授权下读写成功；额外参数被适配器拒绝 |

Lean 探针完成了编译、文件读写与子进程调用，本轮没有发现由 Lean 语言导致的阻碍。

测试使用固定响应的本地工具调用序列，没有额外模型推理。CLI `exec` 路线实际采用 `never`，桌面同类 app-server 路线确认采用 `on-request`；二者的未通过结果均保留。用户随后以 D7 允许 harness 托管薄适配进程；MCP 探针可作为正式接入路线的依据，完整接口仍待后续实现。

**后续直接按完全访问权限＋[访问规则文本](../axiward-prototype-access-rules.md)推进 M0-L2。** 原生隔离、其他任务并行时的隔离以及全部工具通道覆盖，仍属于后续补验项。没有将这些未完成项记为通过。

本目录保留 Lean 探针和试验驱动源码。原始记录位于同一工作区 `work/m0-h1/evidence`，最终摘要与关键记录另附于本目录 `evidence`。

实验已结束并停止辅助进程；原生 sandbox 留下的试验目录拒读登记已清空，临时项目自动登记的信任项已移除。没有安装全局 MCP 入口。
