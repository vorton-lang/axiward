# Axiward 交付目标与实施路线图

2026-09-18 · R0、验收项目、Q0、总体架构与路线已确认；优先验证 Lean 4。具体接口与接入能力按探索结果继续细化。

## 已有依据

- [系统设计 #1](https://github.com/vorton-lang/axiward/discussions/1)：71 条系统设计已确认，导航的首版实现仍为暂定方案。
- [CLI 与接入设计 #2](https://github.com/vorton-lang/axiward/discussions/2)：交互选择已确认，命令名称、数据格式和实际接入仍需细化。
- 本地仓库已加入执行、精化、规格变更和复用；当前范围见 [M1 变更与复用 PDCA](axiward-m1-revision-results.md)。
- M0 接入验证已完成当前原型所需部分；原生强制隔离仍按 D6 后补，实际记录和用户回调已完成探针验证。

R0、总体架构和阶段路线已由用户确认。具体实现细节与接入结论仍需探索和核验，不把总体认可表述为这些能力已经成立。

## R0：首个交付目标（已确认）

交付一个可实际使用的 Axiward 版本：在当前 Windows + Codex 环境与一套固定的验证工具链上，让 agent 从用户确认的规格出发，通过受控工作包交付一个小型软件项目。

验收要求：

1. 精化、执行、探索、请求决定四类流程能够真实运行，并遵守既定的准入和结果接纳规则。
2. 正式成果绑定实际源码、当前规格和经机器核验的证明；根与必要过程义务闭合后才宣布完成。
3. Axiward 状态内核的实现接受机器核验；Git、harness、验证器等外部设施的可信假设和实际调用对应关系明确。
4. 暂停恢复、失败留档、规格变更后的复核与复用，以及不同节点并行，都符合已有设计。
5. 用户负责确认规格、必要决定和最终验收，agent 负责完成项目工作。

验收项目与 Q0 行为规格已确认。用户选择优先验证 Lean 4；最终实现栈与具体版本仍以最小实验的结果为依据，导航实现尚未定稿。

## D1：首个验收项目（已确认）

选择有界 FIFO 队列库，作为 Axiward 的首个验收项目。交付目标仍然是 R0 所定义的 Axiward 版本。

## Q0：队列行为规格（已确认）

1. 提供单线程内存队列。创建时指定固定容量 N，允许 N 为 0，初始队列为空。
2. 未满时入队成功，新元素按原值加入队尾。
3. 已满时入队明确失败，队列内容与顺序不变。
4. 非空时出队成功，返回并移除队首元素，不改写元素。
5. 空队列出队返回“空”，队列不变。
6. 长度查询等于实际元素个数；所有操作后均满足 0 ≤ 长度 ≤ N，容量保持不变。

这些是已确认的人类可读行为要求。M0-L1 已给出 Lean 规格候选、实际队列实现和机器核验的证明，逐项对应关系见 [实验报告](axiward-m0-l1/README.md)。机器核验不替代对规格翻译含义的审阅；本轮样例也不等于经 Axiward 完成了 R0 交付。

## D3：验证路线优先级（已确认）

优先验证 Lean 4，分别检查：Q0 的表达与证明、实际可执行实现的对应关系、对候选证明的可信审查。该选择不自动确定整个 CLI 都用 Lean 编写，也不自动接受未核实的编译或隔离假设。

## D5：CLI 实现语言（用户暂定）

2026-09-18，用户决定 CLI 暂定使用 Lean 开发。若开发中发现由语言或工具链导致的实际问题，及时说明问题、影响与证据，由用户评估是否调整。继续既有 M0–M3 路线；不因选用 Lean 跳过外部设施建模与接入验证，也不自动限定所管理项目的实现语言。

## D6：初版访问约束（用户确认）

2026-09-18，用户明确：沙箱接入问题不阻塞推进，初版可以使用完全访问权限加访问规则文本，继续原计划和 probe。

据此，当前原型依照 [访问规则](axiward-prototype-access-rules.md) 工作。系统的目标设计不变；原生权限强制与对恶意候选的进程隔离暂未闭合，不能把原型验证表述为这些保障已经成立。

## D7：harness 托管适配进程（用户允许）

2026-09-18，用户允许由 harness 托管、随 harness 启停的薄适配进程。适配器只转发限定调用，不管理项目状态或独立后台任务，Lean CLI 仍可按次执行。MCP 已有最小探针；正式接入可采用这一路线。D6 继续有效，不因适配器或沙箱后续完善而暂停当前 probe。

## 总体路线（已确认）

| 节点 | 要交付什么 | 通过条件 | 前置条件 |
| --- | --- | --- | --- |
| M0：验证关键接入 | harness 接入证据，以及实际产物的验证路线 | 实测仓库拒读与受控 CLI 访问；能够绑定真实执行记录并通知指定任务；验证工具能检查目标产物及证明 | R0 确认；验证路线还依赖验收项目的选择 |
| M1：建立状态内核 | 可核验的状态转换实现与 Git 持久化 | 操作准入、节点占用、证据版本和正式状态更新的不变量成立；证明对应实际实现 | M0 的相关前提成立；核心规范完成形式化 |
| M2：接通工作流程 | 文件视图、资料检索、四类工作包与用户决定入口 | 所有正式变更经过内核；实验与用户决定使用正确来源；失败和暂停保留恢复所需状态 | M1；所需 harness 通道可用 |
| M3：完成首个交付 | 一个真实小项目的源码、证明和完整推进记录 | 达到 R0 的验收要求；错误实现、篡改产物、失效证据不能被错误接纳 | M2；具体项目规格已确认 |

M0 的 harness 调查与验证工具链调查可分别推进。后续节点只使用已获得支持的前提；未通过的前提保留为阻碍，不改写为成功。

M0 已细化为 [探索工作包](axiward-m0-work-packages.md)：Lean 实现与证明、权限隔离、可信证明接纳、执行记录、用户决定与恢复。各项当前原型验证结果见 [M0 汇总](axiward-m0-results.md)，强制隔离与正式 UI 欠项继续保留。

对应的职责与数据流见 [CLI 架构图](axiward-cli-architecture.md)。图是逻辑模块划分，不预先要求额外服务或软件包。

这是已确认的总体依赖顺序。每个里程碑还需精化成可一次完成或有界探索的工作包，不能直接把整个里程碑视为 one-shot 执行任务。

## 后续交付节点

| 节点 | 交付内容 | 完成依据 |
| --- | --- | --- |
| M1.1：核心规格 | 状态、命令、权限、证据类型及不变量的机器表示 | 与已确认系统规范对应，信任前提明确 |
| M1.2：状态转换实现 | 准入、占用、接纳、拒绝、取消与闭合的可核验实现 | 实现满足 M1.1，非法转换不能产生正式结果 |
| M1.3：持久化对应关系 | Git 快照、并发更新、恢复和证据版本绑定 | 实际存取符合内核模型，错误或重复提交不覆盖正确状态 |
| M2.1：视图与发现 | 固定输入视图、目录、检索及按需导出 | 读取有授权依据，索引覆盖未导出的可读材料 |
| M2.2：工作包流程 | 四类动作、记录接入、失败返回与暂停恢复 | 模型遵循分派类别，真实记录与解释分开保存 |
| M2.3：人类决定与变更 | decide、通知和根变更后的复核 / 复用 | 决定来源与版本正确，影响只沿真实依赖传播 |
| M2.4：调度与导航 | 节点调度、稳定的评价接口和首版可用策略 | 能推进给定任务；效率由实际结果评价，不视作证明 |
| M3.1：正常交付 | Q0 队列的实现、证明和实际可用产物 | 当前规格及必要过程义务闭合 |
| M3.2：错误与变更验收 | 错误实现、失效证据、越界操作、伪造决定与规格变更案例 | 拒绝错误接纳，保留历史，正确复核和复用 |
| M3.3：持续工作验收 | 多节点并行、暂停恢复与失败后的后续推进 | 占用不自动过期；互不依赖的工作可继续；恢复所需状态完整 |

以上是交付节点，不是已获通过的证明节点。具体验证器接法、接口格式与导航策略，要依据 M0 结果继续确定。

## 人工模拟执行方式

每个工作包记录：目标节点、动作类别、输入版本、允许的影响、提交物、验收条件、实际结果和证据引用。

每轮从精化、执行、探索、请求决定中选一类。发现证据不足或路线不成立时，保留失败并重新导航；不能通过改写完成标准把失败变成成功。

证据分别标明来源：用户确认、机器核验、实际运行观测、人工审阅、未验证建议。人工审阅和测试通过不标记为形式证明完成。

主观验收项依据根规格明确授权的判断主体处理；批准记录绑定产物、规格版本和验收项。机器核验记录与流程，不能把它表述为对主观品质的独立证明。

## 当前状态

| 项目 | 状态 |
| --- | --- |
| 基线资料与仓库现状 | 已核对 |
| D0：确认 R0 首个交付目标 | 已确认 |
| D1：选择首个验收项目 | 已确认：有界 FIFO 队列库 |
| D2：确认队列行为规格 Q0 | 已确认 |
| D3：验证路线优先级 | 已确认：先验证 Lean 4 |
| D4：总体架构与路线 | 已确认；具体实现仍须验证 |
| D5：CLI 实现语言 | 用户暂定 Lean；语言导致的实际问题须及时说明并交由用户评估 |
| D6：初版访问约束 | 完全访问权限＋访问规则文本；沙箱接入暂不阻塞 probe |
| D7：harness 托管适配进程 | 用户允许薄适配器随 harness 启停；无独立后台项目调度 |
| E0：工具链资料与本地环境调查 | 已完成只读调查，记录见 M0 工作包文档 |
| M0-L1：Lean 实现、证明与构建 | 已通过本轮验收，版本固定为 Lean 4.34.0；记录见实验报告 |
| M0-H1：原生访问隔离 | 已获得部分接入证据；强制隔离后续补，依据 D6 不再阻塞原型 |
| M0-L2：证明接纳与绑定 | D6 原型前提下对照通过；固定目标、公理、运行替换与版本绑定已实测，恶意进程隔离后续补 |
| M0-H2：执行记录 | 原生流、真实退出码、绑定与截断对照已通过 |
| M0-H3：用户决定与恢复 | 原生协议对照已通过；用户侧由测试客户端模拟，正式 UI 与重启恢复留待后续 |
| M1 | 执行、嵌套精化、规格变更、节点及历史产物复用已验证；探索和用户决定等流程约束仍需补齐 |
| M2–M3 | 尚未完成 |

M0 结果及下一工作包见 [原型验证汇总](axiward-m0-results.md)。M1 已跑通执行、精化、变更与复用，见 [本阶段 PDCA 与下一交付点](axiward-m1-revision-results.md)。完整状态规则已审核并发布为 [Discussion #3](https://github.com/vorton-lang/axiward/discussions/3)。后续补齐探索、用户决定、导航与原生工作流程接入；隔离欠项单独保留，不阻塞原型。
