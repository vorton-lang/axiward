# M1 多任务精化：验证结果

2026-09-18。已完成本轮交付点：把根目标拆成子目标，允许继续嵌套和独立推进，经核验后自动逐层组合。

## 已实际跑通

本次 FIFO 项目有 **5 个节点、3 个执行叶子、17 次正式转换**。三个叶子分别完成创建、入队、出队与查询要求，系统先关闭中间目标，再关闭根；最终产物已从 Git 导出并运行。

- [实现与规则对应](<USER_HOME/Desktop/axiward/docs/execution-slice.md>)。
- [完整集成结果](<LEGACY_WORKSPACE/work/m1-graph/run-1789700212947/verified/results.json>)与[原始命令记录](<LEGACY_WORKSPACE/work/m1-graph/run-1789700212947/verified/commands.json>)。
- [组合交付程序](<LEGACY_WORKSPACE/work/m1-graph/run-1789700212947/verified/combined-delivery/product/.lake/build/bin/fifo_demo.exe>)。

组合项目的交付提交：`764fd3357680a2aa3993625c719cef100fa25ac314623b11b8ffbbeba3850257`。该项目是验证用的受管仓库；Axiward 源码改动仍在本地工作区，本轮没有源码提交或远端推送。

## 证明与检查

构建、内核声明审查和 `leanchecker Axiward` 全部通过。审查了 **1233 个内核声明**，没有 `sorry`、额外公理或未经核验的运行实现替换。本轮相关定理仅使用 `propext`、`Quot.sound`。

新增性质包括：精化关系无环；每个节点满足执行内核不变量；父结果绑定当前子成果与路线；发布组合结果必须取得匹配的核验通过结果；执行者不能直接批准精化或发布组合结果。

**13 组实际集成检查全部通过。** 在原执行检查之外，新增或加强了：

- 不支持的根策略在初始化时明确拒绝，不会被改成内置的六项要求。
- 多层拆分、不同节点同时领取任务，以及两级自动组合。
- 漏掉一项根要求的拆分被拒绝，子节点不会被创建。
- 各自通过、但针对不同队列实现的子证明不能直接合并。
- 替换路线保留已有子成果；尚未完成的根仍保持未闭合。
- 提交完成后的重试、组合请求 ID 冲突，以及失败后的显式重新核验。

当前 CLI SHA-256：`3a8d7956e58757a983247a3cc41cf4b9cb6183ac4160698caba9c4c3606e7240`。

## 范围与后续

当前是固定 FIFO 验证器：六项要求可以分为不重叠的组，所有子证明绑定同一份队列源码。精化只创建新节点；已有节点共享、成果复用、更完整的规格变更流程、探索、用户决定与导航尚未完成。

持久化已升级为 schema 2；旧 schema 1 实验保留，当前程序会明确拒绝加载，不自动改写旧记录。隔离仍采用用户批准的访问规则前提；Git、工具链和外部调用的信任边界没有被取消。

下一交付点是已有成果的图内复用，以及需求变化时对受影响部分的处理。当前未发现阻塞 Lean CLI 路线的语言问题。所有测试目录保留，没有批量删除。

