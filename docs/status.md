# 项目状态

**当前：R0 控制流程已实现；普通受管 Git 工作区与 worker 显式领包已修改，按用户要求未编译或运行检查。真实 agent 实战尚未启动。**

| 你需要知道什么 | 当前事实 |
| --- | --- |
| 已能做什么 | 按明确规格管理四类工作包，核验实际程序，保存历史，恢复中断，变更规格并复用成果。当前验证器支持 Lean FIFO 两个规格。 |
| 访问如何控制 | 薄适配器与原生权限已经接通。普通命令不能读写正式仓库；复制 CLI 不能伪造答复；核验中的代码不能读取仓库或改写规格。 |
| 已有验证 | 最近一次源码构建、内核审计、证明重放与五组完整流程检查通过。完整回归复验未完成；用户答复由测试客户端模拟，没有模型推理。 |
| 下一项产品工作 | 围绕本机独立 CLI 继续明确多包生命周期与源码合入规则。检查及其性能议题暂缓。 |
| 距最初目标还差什么 | 要接下“做一个 cool PL”这类需求，还需规格生成接入和相应项目验证器。节点与动作选择由 worker 负责。 |

本页用于快速掌握状态；目标见[已审核设计](design.md)，细节见[路线图](roadmap.md)和[权限边界与检查](permissions.md)。

仍需保留的验证边界：原生权限与问答检查的最近通过记录早于最后一次闲置字段删除，之后未重跑原生入口检查。较早版本的完整工程验收不能替代当前版本的完整回归。

运行限制：同项目的核验工具会排队。已观察到单轮构建、审计和证明重放合计约 16–21 秒；多层组合会连续触发多轮。完整回归曾在 150 秒上限超时，延长等待后复验由用户停止，性能问题仍未解决。后续重型检查先说明范围和成本。
