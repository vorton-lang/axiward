# 控制系统生命周期审视 · 会议纪要

日期：2026-09-18—19。更新至：2026-09-19。状态：修复已提交，其余生命周期议题继续讨论。

本页保留本次审视的确认事项、待决边界和行动项，防止把 agent 的解释当成用户批准，或把已发现的缺口遗漏。已审核设计仍见 [design.md](design.md) 与 [kernel-contract.md](kernel-contract.md)；本纪要中的待讨论内容不直接改写实现依据。

## 本轮结论

| 事项 | 当前结果 |
| --- | --- |
| `status` 快照一致性 | 已提交 `989e282`：同一响应从一次加载的 Git 状态生成。 |
| Worker 完整交接 | 已提交 `b84d49801d558fc00a735e4bb36b77de28ba6c87`：按任务提供完整 `handoff`，删除当前 `acknowledged` 标记及工作侧已读接口，保留旧日志必要的解码与回放。 |
| SID 竞态规避 | 已提交 `4a81d89f7e5e47b244b9470c62221af49aca9869`：Axiward 核验启动协调，受限子进程就绪后放行；不同项目的核验主体仍并行。没有修改 Codex 源码。 |
| 检查耗时与有用性 | 用户要求先放下，不再运行检查；未应用会话中提出的 `run-checks.py` 超时/进度 diff。 |

用户要求核对两个修复的提交状态；只读 Git 记录确认提交内容符合所给命令，已回复“是”。本次纪要更新只整理已有事实，不启动检查。交接修复不包含自动接管仍被另一身份占用的活动包；SID 规避也不等于修复 Codex 自身的共享缓存缺陷。

## 已明确的范围与原则

- 本次审视对象是控制系统的完整生命周期。完整产品的扩展能力与控制系统自身的实现、验证分别讨论。
- 用户提出：一个由 Axiward 控制的项目，其整个状态应由 Git 管理，并由 Axiward 控制。
- 仓库同时保持人类可读，在 GitHub 上能够像普通项目一样浏览；用户认可当前仓库结构。
- 尚未提交给 Axiward 的内容不由 Axiward 负责，本次不讨论其保存和恢复。此前将未提交草稿视为可能缺口的判断不再适用于已明确的责任范围。
- Git 有自身状态，Axiward 状态建立在 Git 之上；下一步明确两者的状态与职责分工。
- 用户明确：只读不修改状态，读取无需经由 Git。用户和 discussion 类 agent 的浏览需求不要求放宽 worker 的沙箱；直接只读 Git 命令不再作为待决权限问题。
- Worker 永远不需要读取完整仓库，使用 Axiward 提供的工作视图。用户可以直接打开仓库；discussion 类 agent 在不受 worker 沙箱限制的环境中读取即可。
- 仓库可读性要求是保留普通项目内容与结构；Axiward 不额外加密、压缩打包或封装仓库内容，使用户及 discussion agent 无法直接浏览。
- 用户明确：不能默认下一轮仍是同一个 worker 或仍保有此前上下文；一个 worker 的已读状态不能意味着接续 worker 已经知道相关用户决定。
- 用户提出的常见使用方式是每个工作包使用全新 worker，避免上下文腐化；仅少量场景（如失败重试）继承 worker。交接接口需按接收者没有此前上下文设计。
- 主会话进行会议讨论，由 agent 持续维护纪要。实现改动不因讨论中的建议自动获得授权。最新指令为暂缓检查相关议题，不再运行检查。

## 议题 1：项目仓库应保存什么

用户认可当前仓库结构，要求保持人类可读；下列内容分类尚未逐项审核。未提交内容已明确排除在 Axiward 责任之外。

Agent 提出的内容清单：

| 类别 | 内容 |
| --- | --- |
| 规格与规则 | 已确认需求、形式化命题、操作权限、验证器契约与依赖版本。 |
| 精化图与工作状态 | 目标、依赖、路线、工作包归属和阶段、暂停原因、在途操作。 |
| 工作材料与成果 | 候选源码、证明、分解方案、探索报告、已接纳产物。 |
| 证据与尝试历史 | 实际输入输出、核验结果、失败原因；模型解释单独保存。 |
| 用户决定 | 待决问题、答复、适用版本与范围；当前不保存已读状态。 |
| 交付记录 | 交付物与规格、证明、具体版本的绑定，以及完成依据。 |

### 当前实现事实

- Git 中的 `product/` 保存已接纳产品，`.axiward/` 保存控制数据；正式状态转换将状态与关联材料一起提交。依据：[Git.commitChange](../Axiward/Git.lean)。
- 未提交草稿和 worker 接入配置目前位于 Git 外的工作视图。依据：[工作视图与会话创建](../Axiward/Interface.lean)。用户已明确不要求 Axiward 保存未提交草稿；接入配置不能因此自动归为无须保存的内容，其职责按后续分工判断。

## 议题 2：什么状态归 Git，什么状态归 Axiward

以下为 agent 提出的分工，等待讨论：

| Git 管理 | Axiward 管理 |
| --- | --- |
| 文件内容、目录树、对象标识与版本快照。 | 规格、规范、目标、精化与证明依赖，以及材料在项目中的含义。 |
| 提交历史、父子关系、分支与引用。 | 当前路线、工作包归属与阶段、终稿绑定、尝试结果。 |
| 对象保存、按版本读取、引用的条件更新。 | 操作准入、权限、核验与接纳、失效传播、请求幂等。 |
| 保存承载控制状态的文件，与源码和证据一起形成快照。 | 用户决定、在途操作、暂停恢复、成果有效性与完成条件。 |

- Axiward 的状态和必要依据以文件及内容引用存入 Git；恢复时从正式提交重放并检查。它不维护另一份可独立漂移的权威状态。
- Git 决定内容属于哪个版本；Axiward 判断该版本中的证据是否支持当前目标。文件进入 Git 本身不产生核验通过或目标完成的语义。
- 一次正式转换先由 Axiward 检查并构造状态与材料快照，再由 Git 按预期旧版本更新正式引用；更新成功后才对外报告转换生效。并发冲突后的重新检查属于 Axiward。
- Git 的版本切换不能代替 Axiward 对过程义务和已发生外部操作的处理。

以上分工用于讨论，尚未形成新的实现改动授权。

### 直接操作 Git 的边界

- 用户提出：Axiward 管理的项目应禁止裸操作 Git，并征询 agent 是否同意。
- Agent 同意禁止绕过 Axiward 改变受管项目正式仓库的状态。直接提交、合并、重置、改写分支引用，以及 GitHub 网页编辑或直接合并 PR 等等价写入，都应受同一约束；Axiward 内部仍可调用 Git 实现持久化。
- 用户已明确：只读不修改状态，无需为用户和 discussion 类 agent 的普通浏览设置控制入口；worker 仍使用受限工作视图。Agent 此前将只读 Git 命令作为需进一步确认的问题，已撤回。
- 本项讨论针对绕过控制系统修改状态；仓库保持可直接阅读，不为此放宽 worker 隔离。会议中的原则确认不代表对应权限实现已经完成或已授权立即修改。

## 议题 3：Axiward 自身开发与受管项目的指代

用户明确：

- `axiward` 仓库代表 Axiward 自身的开发工作，不代表 FIFO 等受管项目的开发工作。
- Axiward 自身的开发工作将来也要由 Axiward 管理，因此存在自我指涉；讨论必须区分软件、运行中的控制器和被管理的开发项目。

采用以下名称说明当前理解：

| 指代 | 含义 |
| --- | --- |
| Axiward 软件 | 正在开发的控制系统产品，包括其源码、规格及验证。 |
| Axiward 源码仓库 | 当前 `axiward` 开发仓库，保存 Axiward 自身的开发工作。 |
| Axiward 控制器实例 | 某个确定版本正在运行的程序，负责管理一个目标项目。 |
| 受管项目仓库 | 被该控制器管理的开发工作；可以是 FIFO，也可以是 Axiward 自身。 |

FIFO 项目的规格、候选、证明和交付物属于 FIFO 受管项目。Axiward 源码仓库中的 FIFO 测试夹具用于验证控制系统，不因此把控制系统开发和 FIFO 项目开发合为一件工作；本地测试运行目录的位置也不改变其所属项目。

Agent 对自我管理关系的解释：正在运行的 Axiward 版本 A，可以管理 Axiward 源码仓库中候选版本 B 的开发。A 是控制者，B 是被开发和核验的对象；修改 B 的源码不等于修改 A 当前执行的规则。B 的接纳、交付与替换当前控制器是需要区分的事件。具体如何切换控制器、如何引导首个受管版本尚未讨论，不记为已批准方案。

## 议题 4：Axiward 的配布与产品组成

用户明确：作为控制器使用的 Axiward，不能以源码仓库作为运行配布形式，应以二进制或可运行的软件包存在。源码仓库仍承载 Axiward 本身的开发工作。

用户询问完整产品可能包含哪些部分，列举 CLI、manual、配套 skill、adaptor。以下是 agent 提出的组成清单，尚未逐项批准；逻辑组成不要求拆成独立程序或引入新的扩展框架。

| 部分 | 职责 |
| --- | --- |
| 控制器与 CLI | 编译后的状态内核、流程、权限检查、Git 持久化和用户管理入口。 |
| Harness adaptor | 将 agent 工具、用户问答、原生执行记录和权限配置接入控制器；首个接入是 Codex。 |
| 验证接入与运行资源 | 已支持验证器的调用与结果绑定、所需模板和契约定义；可编入控制器或作为配套资源。 |
| 人类 manual | 安装、项目接管与使用、状态含义、用户决定、恢复与交付等说明。 |
| Agent 配套 skill | 指导需求与规格协作、工作包操作、候选准备、诊断读取和提问；不提供核验放行权，也不代替权限机制。 |
| 安装与版本信息 | 准备运行环境和 harness 接入，标明软件版本、兼容范围、依赖与内容标识。 |

Agent 建议：

- 配布包在不保留 Axiward 源码仓库的机器上也应能安装和运行。若依赖 Git、Codex 或项目验证工具链，须明确声明并检查；是否随包提供运行时和工具链尚待选择。
- 已安装的控制器版本与受管项目的可编辑源码应独立，尤其在管理 Axiward 自身开发时；候选源码修改不应直接改写运行中的控制器。
- 项目自己的规格、历史和交付物属于受管项目；产品配布包提供管理与核验这些内容的能力。FIFO 可作为示例或检查夹具，不作为每个项目的固有内容。

当前实现事实：已有 Lean 编译的 CLI、Python 薄适配器、内置 FIFO 验证接入及生成的操作说明。现有 [使用说明](product-guide.md) 仍从源码目录启动，并要求显式提供 Python 和适配器路径；尚未据此证明独立软件包的安装与运行。配套 skill 的最终内容、安装形态和控制器升级方式尚未确定。

### Windows + Codex 发布范围内的实现差距

用户明确以 Windows + Codex 作为发布要求，并要求此处不计测试待办。下列清单沿用此前明确的自我管理目标；它是 agent 对当前差距的评估，不是新的实现排期，也不把所有可能的语言、平台和验证器列为必做项。

| 差距 | 当前依据与需要补齐的部分 |
| --- | --- |
| 独立运行包与接入初始化 | 已有 CLI 和适配器；缺少脱离源码目录的配布组织、依赖与运行资源定位、面向安装包的 Codex 接入流程。现有 `session` 仍要求显式传入 Python 和适配器脚本绝对路径。 |
| 配套 skill、规格协作与发行版 manual | 已有生成的操作说明和源码使用指南，但仓库中没有可配布的 Axiward skill；需求形成规格并交给用户确认的工作流尚未接通。manual 需要覆盖实际发行版的安装和使用。 |
| Axiward 自身项目的规格与验证接入 | `FifoPolicy` 只接纳两种固定 FIFO 根规格，候选固定为 `Queue.lean` 和 `Proofs.lean`，精化按六项 FIFO 要求分组。需接入 Axiward 自身多文件项目的规格、构建、核验及所需操作契约，才能成为其开发控制器。当前探索仅调用 FIFO 候选核验，用户问答仅记录偏好；更广的实验、规格确认与用户验收事实需按实际项目需求明确并接入。 |
| 既有仓库接管与 GitHub 流转 | 现有入口只在新目录初始化 SHA-256 裸仓库，没有接管当前开发仓库并保留原有历史的流程，也没有与禁止绕过 Axiward 写入相配套的远端同步入口。GitHub 的具体接入与对象格式方案尚待核对和选择；此处不宣称已有兼容结论。 |
| 自我管理时的控制器版本切换 | 当前会话绑定控制器与适配器路径；尚无完整的已安装版本与候选版本交接流程。需明确切换时在途工作和状态格式如何处理；不因此预设自动更新器或要求迁移所有历史格式。 |

用户纠正了此前清单中的“权限规则对齐”：worker 不需要读取完整仓库，用户与 discussion agent 可在各自环境直接浏览。Agent 将不同角色的读取需求混淆，该项缺口已撤回；不据此放宽 worker 权限或新增 discussion 读取机制。正式 Git 写入统一受控的要求仍归入仓库接管与同步流程。

上述差距与测试通过与否分开记录。现有 CLI、状态内核、Codex 薄适配器、四类工作包和 Git 持久化是可继续使用的实现基础。

## 议题 5：控制器与 worker 的交互生命周期

用户当前理解：worker 在受限沙箱内工作，看到 Axiward 提供的工作视图，做四选一，操作，然后提交审核。

以下记录当前实现的实际顺序，不将实现现状自动视为用户已确认的最终交互设计。依据：[Interface.next / exportView](../Axiward/Interface.lean)、[MCP worker 工具](../adapter/server.py)、[Controller.check](../Axiward/Controller.lean)。

1. 已创建的原生任务绑定 worker 身份和受限工作视图。每个新包可以使用全新 worker；继续使用原身份也不依赖此前上下文。控制器与薄适配器不自行调用模型或创建下一轮模型会话。
2. `status` 返回项目概要、节点、四类动作推荐和完整项目层交接。这是动作选择前可取得的信息。
3. `next` 默认由控制器按机械评分选择节点和动作；worker 也可以显式传入合规的节点与动作。控制器核准领取后固定工作包归属、动作与输入快照，再导出该包的详细工作视图。默认调用优先返回该 worker 已有的活动包。
4. Worker 在视图中读取规格、目标、交接和操作说明，编辑候选；通过 `search` / `view_add` 补充固定输入材料及明确标记版本的当前记录。领包后不能在包内更换动作类别。
5. 按动作类型调用对应工具，由控制器和受保护适配器处理正式操作：

| 动作 | 当前流程 |
| --- | --- |
| 执行 | 编写实现与证明 → `submit` 封存并核验 → 接纳产物、闭合目标，或拒绝并结束包。 |
| 精化 | 编写方案与组合证明 → `submit` 封存并核验 → 安装子目标、路线或复用结果；新子目标仍待完成。 |
| 探索 | 编写方案 → `prepare` 准入 → 按预算 `experiment` 并登记真实结果 → `conclude` 提交报告、结束包。探索本身不闭合产品目标。 |
| 请求决定 | 编写具体问题 → `submit` 核准登记 → `ask_user` 走独立用户通道 → 保存答复 → 每次相关交接持续提供答复，无须确认已读。当前答复仅产生版本绑定偏好。 |

6. Axiward 保存转换、结果和证据，核验可组合的父目标；worker 根据返回结果进入下一轮。拒绝后修改候选需领取新包；核验中断用 `resume` 继续原封存终稿。项目完成、暂停或等待时，返回对应状态，不能把工作包结束等同于项目完成。

本轮要明确的区别：选择前的项目概要与领取后的详细工作视图不同；默认四选一由当前机械导航完成，worker 可显式提出另一合规选择；探索和用户决定并非全部操作做完后才第一次审核。

### Worker 初始目录与补充材料

用户进一步核对：worker 出生在空白目录，调用 `status` 获得项目地图，领包后获得工作视图，并可额外签出合规文件。

当前实现支持这一整体顺序，具体边界如下：

- “空白目录”指初始不含项目源码和工作包材料的专用视图目录；`session` 已放入 `.codex/config.toml`、`AGENTS.md`、`START.md`，并创建 `work/` 与 `tmp/`。
- `status` 返回项目概要，`next` 核准领包后生成 `work/packages/<node>-<serial>/` 下的规格、目标、操作说明、材料目录及候选目录。
- 额外材料经 `search` 发现、`view_add` 导出到本包 `materials/`。`node/` 材料来自领取时固定快照；`current/` 记录绑定当前返回版本。两者均受现有访问规则约束，worker 不直接操作 Git。
- 当前导出限于控制器资源目录中已列出的规格、目标、历史、证据、先前候选和报告，尚不是按任意仓库路径签出文件的通用接口。依据：[Interface.resources / addResource](../Axiward/Interface.lean)。

### Worker 的 `status` 如何生成

用户询问返回内容的生成方式。当前实现事实：

- Worker 调用的是 MCP `status`。最初核对时，适配器分别调用 CLI 的 `overview`、`navigate`、`inbox` 并组合结果；经已批准的 `989e282` 修复，现改为一次调用 `worker-status` 统一生成。它与直接运行 CLI `axiward status` 的输出不同。
- 组合入口通过一次 `Git.load` 读取正式 `main` 提交中的 `.axiward/state.json`。文件存储初始规格与转换记录；`Engine.restore` 重新计算纯状态转换并核对记录结果，随后检查规格、候选、路线、产物及相关证据的 Git 内容绑定。此过程不重做历史实验或重新运行全部证明核验。
- `status` 部分由恢复出的状态计算：提交 ID、完成/暂停状态、在途操作数、转换次数和项目地图。地图逐个列出节点编号、要求 ID、当前适用性、闭合状态、活动包、阶段和子节点。
- `navigation` 对当前路线上的节点运行固定机械规则，根据可领取条件、已有路线、要求数量和近期失败等信息给出四类动作的 `allowed`、分数与理由；不调用模型，也不把分数解释为成功概率。
- `b84d498` 已用 `handoff` 替代工作侧 `inbox`，按任务、版本和权限计算完整交接，不再按提问者或已读状态省略决定。用户决定是否仍适用会根据当前范围与依赖重新判断。
- 返回内容是固定代码生成的结构化 JSON。当前地图没有自动生成的自然语言项目介绍。
- 原适配器三次独立加载仓库，导致三部分可能混用版本。用户认定为 bug，随后批准独立修复；现已统一到同一快照。领取与提交的另行检查不替代 `status` 自身的一致性。

依据：[Adapter.call](../adapter/server.py)、[Interface.status / navigation / handoff](../Axiward/Interface.lean)、[Git.load](../Axiward/Git.lean)、[Engine.restore](../Axiward/Engine.lean)。

### 已识别问题及处理状态

Worker 交接、`status` 快照一致性与 Axiward 核验启动协调均已提交。Codex 自身缺陷仍然存在；新方案只协调 Axiward 核验调用。

| 问题 | 触发与影响 | 必须满足的要求 |
| --- | --- | --- |
| Worker 接续时的决定可见性 | 原先按 owner 和已读状态过滤，无法保证新 worker 获得必要决定。 | 已通过 `b84d498` 改为完整任务交接，并删除当前已读状态。 |
| `status` 并发快照不一致 | 原适配器分别调用 `overview`、`navigate`、`inbox`，可能混用版本。 | 已修复并提交为 `989e282`，三个部分从同一正式状态生成。 |

### 全新 worker 与完整交接

用户提出：未来很可能每个包都由全新 worker 执行，仅在失败重试等少量场景继承上下文，因此每次返回所有需要读的交接。用户随后审核批准下述交接实现方案，并明确要求一并去除已无用途的已读标记。

- 正确推进不能依赖 worker 的记忆；即便实际沿用 worker，也应能从本次返回的信息独立理解任务。
- 必读信息按当前任务、目标与版本确定；此前某个 worker 读过，不是省略该信息的依据。
- 已批准并实现：`status` 提供选择下一步所需的项目状态和相关决定；领取或恢复包时提供完整的包级交接，覆盖目标与输入版本、已有成果及依赖、适用用户决定、未解决事项和相关失败信息。
- 详细原始记录可按需读取，但关键约束、决定与待办不能只留一个需要新 worker 自行猜测的历史入口。
- 失败重试可以复用 worker 上下文；按已有终稿规则，需要修改的候选仍领取新包。上下文是否复用不改变交接的完整性要求。
- 用户明确 `acknowledged` 已无用途，可以去除；已批准完整删除该标记及工作侧已读机制，不保留作为备用通知状态。

## 后台复验结果与独立发现

已授权的检查 1 已由一个 subagent 完成。版本为 `724a5b3bb09b39694a3c6523b87da6bf5f603ad9`；11 个检查命令中 10 通过、1 失败、0 未完成，总耗时约 40 分 8 秒，未调用模型推理。

- 通过：构建、公理审计、证明重放、工作流场景、集成流程、MCP 流程、原生权限与模拟用户问答、配置保护、候选核验隔离、同项目并发核验。
- 失败：不同项目并发核验。两个正确候选均在构建阶段因无法创建各自的 `snapshot/.lake/build` 而被拒绝，原始错误为 `permission denied (13)`，不是超时。
- 当时原因尚未证实；随后独立探针已定位到 Codex 共享 SID 缓存的首次并发注册问题，详见下节。项目内锁不能协调该用户级共享资源。
- 此失败与会议中发现的 `status` 混合版本问题独立；初次复验只记录问题，之后分别获得修复授权并提交。
- 检查期间 HEAD 与已跟踪文件字节未变；主会话只新增本纪要。无遗留测试进程，运行证据保留在 `.work/`。

证据：[命令、结果与耗时](../.work/check1-20260918-214613/summary.md)、[跨项目并发原始结果](../.work/concurrent-isolation-1789737982520545100/result.json)。原始证据是本机未入 Git 的运行产物。

## 跨项目核验权限故障调查

用户要求调查原因，并曾要求 bug 说明包含问题描述、原因和拟定解决方案；随后明确停止套用固定格式，后续按实际内容沟通。

### 问题描述

两个项目首次同时启动原生核验时，正确候选因构建目录 permission denied 被拒绝。独立 Python 探针无需 Lean 或 Axiward 状态转换也能复现。

### 原因

已确认目录 ACL 授权的 SID 与进程限制令牌中的 SID 不一致。冷并发时，Codex 用户级 `cap_sid` 缓存条目从 9 降为 1，基础 SID 变化，两边写入均失败；串行注册后映射恢复，再对同一路径并发则成功。故障位于 Codex 原生权限 SID 注册的一致性边界，Axiward 的按仓库锁无法保护该用户级共享资源。尚未审阅 Codex 源码的具体竞态语句。

### 已批准并完成的 Axiward 侧规避

最初提出在 Codex 原生沙箱层协调共享 SID 缓存的读改写。用户明确无法修改 Codex 源码，该依赖层修复不属于我们可执行的方案。

用户随后批准验证 Axiward 侧方案：可行则修复并提交，不可行则报告。实际生产入口已实现同一 Codex 环境下的共享启动锁，受限 PowerShell 子进程发出就绪信号后释放启动锁，再执行固定的 Lake 命令；原有项目内核验锁保留。

生产入口的三个全新沙箱执行区间重叠 1.719 秒，30 轮权限观测符合预期；原先失败的跨项目完整核验、同项目并发及正确/恶意候选隔离检查均通过。规避已提交为 `4a81d89`，没有把不同项目的核验主体全部串行化。

该协调仅约束 Axiward 核验调用，不能自动约束其他独立 Codex 进程；不能宣称 Codex 的用户级共享缓存缺陷已被修复。

用户进一步要求完整 bug 描述，已整理为[完整描述、复现与拟定方案](../experiments/windows-sandbox-concurrency.md)，区分已观察到的 SID 不一致与尚未审阅的源码竞态细节。

依据：[独立复现与生产入口探针](../experiments/windows-sandbox-concurrency.py)、[根因记录](../.work/sandbox-concurrency-cause-20260918-02/results.json)、[生产握手结果](../.work/sandbox-startup-production-20260918-final/summary.json)。生产修复位于 `Axiward/Sandbox.lean`；未修改 Codex 源码、未手工调整 ACL 或读取沙箱密码。

## Bug 修复方式审查（status 方案已批准）

用户先要求判断是否存在唯一合理修复方式并提交具体方案审核；随后明确同意仅修复 `status` 的单快照方案，要求由 subagent 实施并提交。其余问题继续在主会话讨论。

| 问题 | Agent 判断 |
| --- | --- |
| Worker 接续时的决定可见性 | 原先存在信息交接方案选择；用户已批准按任务提供完整交接，并删除已读标记。已提交 `b84d498`。 |
| `status` 混合版本 | 正确性要求明确：同一响应必须对应一个正式快照。实现可以是一次加载后统一生成，或各读取显式绑定同一提交；因此不宣称绝对唯一。一次加载统一生成是当前边界下推荐的最小充分方案。 |
| 跨项目核验权限故障 | 用户明确无法修改 Codex 源码；已批准验证并实施 Axiward 侧启动协调，提交为 `4a81d89`。 |

### 已批准：仅修复 `status` 的快照一致性

以下保留当时的独立修复范围；后续交接提交将响应中的 `inbox` 替换为 `handoff`，仍保持单快照保证。

批准范围：

1. 提供一个 worker 状态读取入口，读取正式 `main` 的某个提交并执行一次 `Git.load`；从同一个 `Loaded` 值生成现有 `status`、`navigation`、`inbox` 三部分。
2. MCP 适配器改为调用这个入口一次，不再分三次启动 CLI 并读取各自的当前版本。沿用现有字段与当前 inbox 语义；worker 身份仍由受保护的启动配置绑定。
3. 沿用 Git 提交不可变的性质处理并发。其他 worker 可以继续提交；响应允许对应读取时的旧完整版本，但不允许混合版本，不新增项目写锁或另一份状态缓存。

预计修改范围为 `Main.lean`、`adapter/server.py`；若组合函数放在现有接口层，则同时涉及 `Axiward/Interface.lean`。验证放入现有相关检查，覆盖并发提交期间三部分都与返回的 `head` 一致，及既有 MCP 返回结构保持可用；不重跑无关的全部重型检查。

验收场景：读取过程中另一个 worker 暂停项目或保存决定，响应中的状态、推荐和答复必须全部可以从返回的同一个 Git 提交重算得到。新版本可由后续查询取得。此方案不修复决定交接或核验沙箱问题。

用户已批准上述 `status` 修复及针对性验证，并要求提交。一个 subagent 已完成并提交 `989e2827ad3eaf80198afb550e6110dab0807a7c`（未推送），仅涉及 `Main.lean`、`adapter/server.py`、`Tests/status_snapshot.py`。旧实现上的针对性回归先按预期失败，修复后构建、同一回归与原生 MCP 检查通过。依据：[验证记录](../.work/status-snapshot-20260918-224211/summary.md)。没有一并修改交接方式或跨项目权限。

## 交接机制实现方案（已实现并提交）

用户审核本节方案后，额外明确已读标记可完全去除，随后批准由 subagent 实施。实现已完成，提交为 `b84d498`；会议纪要与修复提交分开维护。

### 方案：以任务为对象生成完整交接

复用 Git 中已有的目标、路线、工作包、用户决定和操作记录，计算交接视图。使用结构化 `handoff` 取代工作接口中的未读 `inbox`，沿用现有响应和 `view.json`；不持久化另一份交接状态，不新增按 worker 或会话维护的已读表，也不调用模型生成权威摘要。

| 入口 | 必须提供的信息 |
| --- | --- |
| `status` | 当前项目和版本、节点与路线、适用于当前工作的用户决定、待决定事项、暂停与在途操作，足以进行下一步选择。 |
| `next` 领取或重新取得包视图 | 本包目标与动作、固定输入版本、已接纳成果及依赖、相关决定、当前阶段、相关尝试结局与失败原因、在途操作和材料引用。 |
| 封存核验恢复、用户答复等带交接的结果 | 重新生成当时完整的必要交接；不因同一身份、此前送达或已读而省略。 |

### 信息范围与有效性

- 决定不按提问 worker 的身份或 `acknowledged` 过滤。保持决定原有的目标、版本与来源标记，不因展示给另一个 worker 而扩大授权范围。
- 项目层展示当前路线的相关信息；包层覆盖本目标、其当前路线祖先及实际使用的依赖，保留每条信息的适用对象。祖先或依赖上的决定作为带来源的上下文呈现，不自动改写为对子目标的新授权。
- 当前有效决定按已记录的适用性、所绑定目标/版本及当前依赖重新判断。过期、越出既定选项的答复不显示为当前有效指令；历史仍可检索。不能仅凭回答时保存的 `applicable=true` 判断它现在仍适用。
- 对本目标的相关正式尝试保留动作、结局、原因及终稿/证据引用；原始长日志按需读取。关键约束、决定和未解决事项直接出现在交接中，不只给历史链接，也不以任意“最近几条”截断来冒充完整交接。
- 继续遵守现有资料访问规则。若必要交接内容不可见，要明确显示缺失与阻碍，不能静默给出看似完整的交接。

### 快照与生命周期边界

- 一次返回的当前状态与交接复用同一份 `Loaded`，延续刚完成的单快照修复。
- 包的源码、规格和已有材料仍来自领取时固定快照；交接明确区分输入快照与当前状态版本。规格变更、用户后续答复、取消等作为当前变化呈现，不悄悄替换包输入。
- 读交接不写 Git、不消耗消息、不转移工作包归属。它支持每个新包由全新 worker 执行；自动创建 worker 或转交尚被他人占用的包不是本修复新增的机制。
- 失败后换新包或核验中断后恢复原终稿的既有规则保持适用。

### 删除与历史处理

- 删除工作侧 `inbox`/`acknowledge` 的命令、MCP 工具和已读指引，移除当前响应中的未读投递语义，统一使用交接视图。
- 完全删除当前模型中的 `acknowledged` 标记及其更新/过滤逻辑。旧 Git 历史可能已有 `acknowledge` 操作和对应结果，仅保留使这些提交仍可解码/重放的必要支持；不再恢复或维护已读状态。新工作不产生已读操作，不改写既有 Git 历史。
- 此处保留旧重放的具体理由是：直接删除命令类型或结果类型会使现存日志无法解码；并非预建一般化迁移框架。
- 同步更新生成的操作指引、manual 与相关规则说明，去掉必须沿用原会话或读取后必须确认已读的要求。

### 验收与改动范围

验收案例：

1. A 提问并收到用户答复后，完全没有 A 上下文的 B 领取后续相关包，自动得到同一适用决定及必要上下文。
2. 同一身份丢失上下文后重新查询，交接仍完整；重复查询不产生状态变更，历史上已确认已读的决定也不被隐藏。
3. 根规格或依赖变化后，失效决定不会被当作当前指令；包输入保持原快照，当前变化明确可见。
4. 恢复时能看见相关在途操作，不能因更换 worker 而重复执行；跨 worker 的读取不赋予取消或提交别人的包的权限。
5. 并发变化时，当前状态与交接仍对应同一明确提交，保持快照回归的有效性。

实现涉及 `Axiward/Interface.lean`、`Main.lean`、`adapter/server.py`、已读标记相关模型与转换，以及对应检查和说明。保留旧记录重放所必需的最小支持。使用已有流程和原生问答检查验证接合处，增加有实际失败能力的新 worker 交接案例；不扩入核验沙箱或软件配布。

已完成的整体范围为：完整交接生成、工作侧已读机制及 `acknowledged` 标记移除、必要的旧历史可读性、对应说明与相关检查更新。修复没有新增自动创建 worker 或转交他人活动包的机制。

## 已有验证记录与检查议题

以下仅整理停止指令前产生的记录，不是本次纪要更新重新运行的检查：

- 交接修复已有构建、公理审计、证明重放、纯流程、存储恢复、交接、快照和原生问答检查通过记录。
- 最后一次完整 `workflow.py` 已结束，原始记录为退出码 0、702.672 秒，五组场景通过。此前 `summary.md` 的 pending 尚未刷新，应以 [checks.json](../.work/handoff-20260918/checks.json) 和 [workflow/results.json](../.work/handoff-20260918/workflow/results.json) 为准。
- SID 规避已有跨项目核验 79.629 秒、同项目核验 71.404 秒、核验隔离 122.004 秒的通过记录，以及上述生产握手观测。
- 交接/快照/内核检查基于交接改动；后续原生、完整 workflow 与 SID 检查基于两项改动共存的工作区。提交后没有重新执行全套回归，不据此扩大验证结论。

用户质疑长时间检查的必要性。Agent 的静态判断是：`run-checks.py` 只负责编排和日志，没有独立验收逻辑；具体交接与跨组件用例有作用，但整套流程是否需要每次重跑尚需逐项论证。用户要求先放下该议题，整理提交修复，不再运行任何检查。会话中提供的超时/进度 diff 未获实施批准，也未应用；没有借此删除验收目标或宣称性能问题已解决。

## 已授权行动

- 一个 subagent 执行当前版本四组复验的行动已完成，结果见上节。
- `status` 单快照修复已完成、验证并提交为 `989e282`，未推送。
- Worker 完整交接已提交 `b84d498`；SID 启动协调已提交 `4a81d89`，两个提交内容已按用户要求只读核对。
- 检查必要性与性能问题暂缓，未授权继续跑检查或应用 runner diff；发布组成、既有仓库接管、自我管理的版本切换等剩余生命周期议题仍待讨论。
- 已有检查通过不表示本次生命周期审视中识别的其他缺口或待决要求已经满足。
